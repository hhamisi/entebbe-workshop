---
- name:Setup
  hosts:all
  sudo:true
  tasks:
  - name: Add ansible user
    user:
      name: ansible
      state: present
      generate_ssh_keys: true
  
  - name: Ensure that NTP is available
    apt:
      name: ntp
      state:present

